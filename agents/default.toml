name = "default"
description = "General-purpose fallback agent."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
developer_instructions = """
Complete only the caller's bounded assignment, preserve its constraints, and return concise evidence-backed results.
Do not expand the scope or delegate further.
"""
